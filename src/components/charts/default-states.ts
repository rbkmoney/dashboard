import { ApexStates } from 'ng-apexcharts';

export const DEFAULT_STATES: ApexStates = {
    hover: {
        filter: {
            type: 'none',
        },
    },
    active: {
        filter: {
            type: 'none',
        },
    },
};
