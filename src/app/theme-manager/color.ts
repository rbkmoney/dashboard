export enum StatusColor {
    neutral = 'neutral',
    success = 'success',
    pending = 'pending',
    warn = 'warn'
}

export enum PaletteColor {
    primary = 'primary',
    accent = 'accent',
    warn = 'warn'
}
