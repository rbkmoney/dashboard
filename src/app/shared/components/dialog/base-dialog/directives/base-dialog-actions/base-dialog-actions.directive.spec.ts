import { BaseDialogActionsDirective } from './base-dialog-actions.directive';

describe('BaseDialogActionsDirective', () => {
    it('should create an instance', () => {
        const directive = new BaseDialogActionsDirective();
        expect(directive).toBeTruthy();
    });
});
