import { BaseDialogTitleDirective } from './base-dialog-title.directive';

describe('BaseDialogTitleDirective', () => {
    it('should create an instance', () => {
        const directive = new BaseDialogTitleDirective();
        expect(directive).toBeTruthy();
    });
});
