import { InjectionToken } from '@angular/core';

export const DEPOSITS_UPDATE_DELAY_TOKEN = new InjectionToken<number>('deposits-update-delay-token');
export const DEFAULT_DEPOSITS_UPDATE_DELAY = 300;
